<?xml version="1.0" encoding="utf-8"?>
<!-- Where AppUpdateService downloads new APKs (getTemporaryDirectory() is the
     app's cache dir), shared with the system package installer. -->
<paths>
    <cache-path name="updates" path="updates/" />
</paths>
